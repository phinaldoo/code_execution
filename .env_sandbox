# These env vars are copied into a sandbox only when:
# 1. ALLOW_SANDBOX_ENV_INJECTION=true on the gateway, and
# 2. The client creates the container with "inject_sandbox_env": true

STIRLINGPDF_API_KEY=""
OPENAI_API_KEY=""
